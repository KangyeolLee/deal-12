// // get the client
const mysql = require('mysql2');
const mysqlssh = require('mysql-ssh');

// create the connection to database
export const config = {
  host: '3.36.99.206',
  port: '3306',
  user: 'danmin20',
  password: '1108',
  database: 'woowa_db',
};
